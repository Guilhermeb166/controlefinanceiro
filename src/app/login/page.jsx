/**
 * Página de Login (Server Component).
 * Renderiza o componente principal de login e registro.
 */
import LoginMain from "@/components/Login/LoginMain";

export default function LoginPage() {
    return <LoginMain />;
}
